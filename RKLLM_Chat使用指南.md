# RKLLM Chat 使用指南

> 📌 **Windows cmd 查看本文件前请先执行 `chcp 65001`**，否则中文会显示为乱码。

**RKLLM Chat 是一款运行在设备上的本地 AI 对话助手**，无需联网，所有问题都在设备内部完成推理。打开就能用，就像使用一台专属的 AI 聊天机。

---

## 1. 开始使用

1. 给设备接上电源，按电源键开机，屏幕亮起。
2. 打开终端（电脑上用 adb 连接，或直接使用设备终端）。
3. 输入以下命令启动：

```bash
cd /data/local/tmp/android && ./rkchat.sh qwen
```

等待几秒钟，看到如下界面即表示启动成功：

```
╔══════════════════════════════════════════════╗
║     RKLLM Chat  —  RK3588/RK3576 NPU       ║
╚══════════════════════════════════════════════╝
  Model:    Qwen3-4B (通用对话模型)
  Context:  8192 tokens
  Preset:   balanced
  History:  ON
  Thinking: OFF
▸
```

## 2. 对话

- 在 `▸` 提示符后**直接输入问题**，按回车发送，稍等片刻即返回回答。
- 可以连续追问，设备会记住之前的对话内容（多轮对话）。
- 建议一次问一个问题，回答会更快、更准确。

示例：

```
▸ 你好，介绍一下你自己
▸ 帮我写一首关于夏天的诗
▸ 1+2+3+...+100 等于多少？
```

## 3. 常用操作

| 想做什么 | 怎么做 |
|---|---|
| 查看所有可用命令 | 输入 `/help` |
| 开始新话题（清空历史） | 输入 `/clear` |
| 结束使用 | 输入 `/exit` |
| 切换思考模式（回答更深入） | 输入 `/thinking on` |
| 恢复默认设置 | 重新启动程序即可 |

## 4. 界面小提示

- 底部 `[░░░░ 35/8192]` 表示对话记忆的使用量：数值接近上限时，最早的对话内容会被自动遗忘，属正常现象。
- 回答字数较多时生成会稍慢，属正常现象（AI 在设备内部"思考"）。

## 5. 常见问题

| 问题 | 解决办法 |
|---|---|
| 启动时提示找不到模型 | 用 `./rkchat.sh qwen` 启动（已预装 Qwen3-4B 模型） |
| 输入中文变成乱码 | 先输入 `chcp 65001` 回车，再重新启动程序 |
| 对话没反应 | 等待几秒；若长时间无响应，按 `Ctrl+C` 退出后重新启动 |
| 屏幕没反应 | 按电源键熄灭再点亮屏幕即可恢复（若多次无效请重启设备） |
| 想换一个模型 | 启动时加参数：`./rkchat.sh qwen`（4B 模型，对话质量更高） |

## 6. 安全须知

- 本设备为本地 AI，**对话内容不会上传网络**，隐私安全。
- 设备运行时请保持电源稳定，避免意外断电。
- 回答内容由 AI 生成，仅供参考，重要事项请自行核实。

---

*祝您使用愉快！*
